[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "museum-collection-service"
version = "1.0.0"
description = "Collection management service"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
